https://codepen.io/HDPearlman/pen/yLyxWPo?editors=1100

1. What is Semantic HTML? 
        Semantic HTML is using HTML tags and groupings to make your code readable and give it meaning.
    2. What is HTML used for? 
        HTML is for grouping and orginizing your content for a web page.
    3. What is an attribute and where do we put it? 
        an atrribute is a modifier for a tag that can give it more information or a grouping like a class or the source for an image
    4. What is the h1 tag used for? How many times should I use it on a page?
        h1 denotes the most important title on the page ideally it would be used once 
    5. Name two tags that have required attributes
        <img src> <a href=>
    6. What do we put in the head of our HTML document? 
        things like the .css files and information we dont want to display on the page
    7. What is an id? 
        its a specific attribute for an element that allows you to find it later
    8. What elements can I add an id to? 
        any element
    9. How many times can I use the same id on a page? 
        just once it should be unique
    10. What is a class? 
        a class is a grouping that can be called on to apply stylings to multiple things
    11. What elements can I add a class to? 
        <divs> content as of right now im not sure if there are any it cant be used on
    12. How many times can I use the same class on a page? 
        as many as you need
    13. How do I get my link to open in a new tab?
        <a href="https://www.google.com/" target="_blank">google.com</a>
    14. What is the alt attribute used for? 
        it displays text when the element cannot be loaded
    15. How do I reference an id?
        use the # symbol
    16. What is the difference between a section and a div
        a section is used for related content whereas a div does not have inherent meaning and is used more broadly
    17. What is CSS used for? 
        Stylizing your HTML ie changed colors fonts and even adding animation
    18. How to we select an element? Example - every h2 on the page
        h2 {
            css here
        }
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        a class is used for multiple items where ass an id is only used for 1 a class could be used to make square boxes around generic content and an ID could be used to highlight a very specific piece of content that you would not want to be that same style
    20. How do we select classes in CSS?
        . the period
    21. How do we select a p element with a single class of “human””?
        .human
    22. What is a parent child selector? When would this be useful? 
        a parent child selector is an arrrow > its useful if you want to affect 1 element or type of element in a parant without affect other children of that child
    23. How do you select all links within a div with the class of sidebar?
        .sidebar a{
            css
        }
    24. What is a pseudo selector?
        a pseudo selector is a modifier for when to select something and enact that css ie when your hovering over something
    25. What do we use the change the spacing between lines?
        line-height: property
    26. What do we use to change the spacing between letters?
        letter-spacing: property
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        text-transform:uppercase;
        text-transform:lowercase;
        text-transform:capitalize
    28. How do I add a 1px border around my div that is dotted and black?
        border: dotted black 1px;
    29. How do I select everything on the page? 
        * {
            wildcard selector
        }
    30. How do I write a comment in CSS?
        /* comment here
    31. How do I find out what file I am in, when I am using the command line? 
        pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
        ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
        rm filename // it does not go to the trashcan and there is no confirmation message its just gone
    34. Why should I use version control? 
        because it makes it easier to track changes as well as going back to a stable version if something breaks it also makes it so you can save space
    35. How often should I commit to github?
        as often as you would save normally
    36. What is the command we would use to push our repo up to github? 
        git push origin Firstname-Lastname
    37. Walk me through Lambda's git flow. 
        fork
        add Tl as Collaborator
        git clone to local file in terminal
        create a branch git checkout -b "Firstname-Lastname"
        git add if you add a new file either git add filename or git add .
        git commit -m 'message for reason of commit'
        git push origin 'Firstname-lastname' //(branch name)
        select correct branch
        in github compare and pull request TO YOUR MASTER FORK NOT LAMBDA
        add TL as reviewer
        click create pull request
        dont merge thats for the TL
        work on project add comit push cycle continues


Stretch Questions

    1. What is the difference between an inline element and a block element?
    inline element is written in the line of the HTML and the block element is like a full div block.
    2. What happens when an element is positioned absolutely? 
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    5. In your own words, explain the box model. What is the fix for the box model? 
    content>padding>margin>border